const map = require('./map');
const filter = require('./filter');
const scan = require('./scan');
const debounce = require('./debounce');
const startWith = require('./startWith');

module.exports = {
    map, filter, scan, debounce, startWith
}
